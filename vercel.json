{
  "outputDirectory": "www",
  "buildCommand": "npm run build",
  "rewrites": [
    { "source": "/share/:id", "destination": "/api/share/:id" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
